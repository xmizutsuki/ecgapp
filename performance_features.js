/* ECG Lab — Meu Desempenho / My Performance.
   Centralizes local-first learning events from adaptive training, practice exams and CaseCoach.
   Objective statistics are calculated deterministically; AI only interprets structured aggregates. */
(function(){
  const VERSION=1;
  const PERF_PREFIX=`ecgLabPerformance:v${VERSION}:`;
  const SNAP_PREFIX=`ecgLabPerformanceSnapshots:v${VERSION}:`;
  const FOCUS_PREFIX=`ecgLabTrainingFocus:v1:`;
  const TRAIN_PREFIX='ecgLabTraining:v1:';
  const SIM_PREFIX='ecgLabSimulations:v4:';
  const CASE_PREFIX='ecgLabCaseReflection:';
  const PERIODS={d7:7,d30:30,d90:90,all:null};
  const runtime={period:'d30',remoteLoadedFor:null,syncTimer:null,aiBusy:false,renderQueued:false};

  const text={
    pt:{
      title:'Meu Desempenho',subtitle:'Acompanhe sua evolução e transforme seus resultados em um plano de estudo objetivo.',nav:'Meu Desempenho',mobile:'Desempenho',mastery:'ECG Mastery Score',insufficient:'Dados insuficientes',insufficientBody:'Complete mais atividades para aumentar a confiabilidade da estimativa de domínio.',lowConfidence:'Baixa confiabilidade',mediumConfidence:'Confiabilidade moderada',highConfidence:'Boa confiabilidade',questionsAnswered:'Questões respondidas',accuracy:'Taxa de acerto',simulations:'Simulados concluídos',clinicalCases:'Estudos de caso',studyTime:'Tempo total de estudo',streak:'Sequência atual',days:'dias',day:'dia',filters:'Período',days7:'7 dias',days30:'30 dias',days90:'90 dias',allTime:'Todo o período',progress:'Evolução do desempenho',performance:'Desempenho',change:'Variação',noChart:'Ainda não há dados suficientes para mostrar uma curva de evolução.',skills:'Desempenho por competência',skill:'Competência',attempts:'Tentativas',masteryLabel:'Domínio estimado',majorAreas:'Seu desempenho por área',analysis:'Análise do seu desempenho',analysisBadge:'IA interpreta; o sistema calcula os números',opportunities:'Principais oportunidades de evolução',plan:'Plano recomendado',priority:'Prioridade',current:'Desempenho atual',recommendation:'Recomendação',startReview:'Iniciar revisão',trainWeak:'TREINAR MINHAS ÁREAS DE MAIOR DIFICULDADE',trainWeakHelp:'Cria um treino adaptativo de 20 questões priorizando as áreas com menor domínio e mantendo revisão espaçada das áreas já dominadas.',activity:'Sua atividade',studiedDays:'Dias estudados',questionsWeek:'Questões nos últimos 7 dias',avgSession:'Tempo médio por sessão',totalStudy:'Tempo total',currentStreak:'Sequência atual',longestStreak:'Maior sequência',xp:'XP de estudo',xpNote:'XP mede engajamento e não competência clínica.',level:'Nível',refreshAI:'Atualizar análise com IA',aiUnavailable:'Conecte o Supabase e a função de IA para uma interpretação personalizada. A análise abaixo é calculada localmente a partir dos seus dados.',aiLoading:'Analisando seus dados…',aiError:'Não foi possível atualizar a análise por IA.',emptyTitle:'Seu desempenho aparecerá aqui conforme você utilizar o ECG Lab.',emptyBody:'Complete seu primeiro treino, simulado ou estudo de caso para começar.',startTraining:'Iniciar Treino',startSimulation:'Fazer Simulado',openCase:'Abrir Estudo de Caso',improving:'Seu desempenho recente está melhorando.',stable:'Seu desempenho recente está estável.',declining:'Seu desempenho recente apresentou queda.',needMore:'Ainda são necessárias mais respostas para identificar padrões consistentes.',noWeakness:'Nenhuma dificuldade recorrente atingiu o mínimo de três ocorrências neste período.',practice:'Pratique',reviewTheory:'Revise a teoria e depois resolva questões direcionadas.',casePractice:'Complete um estudo de caso relacionado.',focusedReady:'Treino focado preparado com base nas suas áreas de maior prioridade.',completed:'concluídos',correct:'acertos',caseReasoning:'Raciocínio clínico',basicInterpretation:'Interpretação básica',rhythmRate:'Ritmo e frequência',conduction:'Condução',arrhythmias:'Arritmias',ischemia:'Isquemia',pacing:'Marcapasso',clinicalReasoning:'Raciocínio clínico',noEvidence:'Sem dados suficientes',trendUp:'melhora',trendDown:'queda',trendFlat:'estável',objectiveNote:'Percentuais, Mastery Score, XP e tendências são calculados pelo aplicativo. A IA não altera esses valores.'
    },
    en:{
      title:'My Performance',subtitle:'Track your progress and turn results into an objective study plan.',nav:'My Performance',mobile:'Performance',mastery:'ECG Mastery Score',insufficient:'Insufficient data',insufficientBody:'Complete more activities to increase the reliability of your mastery estimate.',lowConfidence:'Low confidence',mediumConfidence:'Moderate confidence',highConfidence:'Good confidence',questionsAnswered:'Questions answered',accuracy:'Accuracy',simulations:'Completed practice exams',clinicalCases:'Clinical cases',studyTime:'Total study time',streak:'Current streak',days:'days',day:'day',filters:'Period',days7:'7 Days',days30:'30 Days',days90:'90 Days',allTime:'All Time',progress:'Performance Progress',performance:'Performance',change:'Change',noChart:'There is not enough data yet to display a performance curve.',skills:'Performance by Skill',skill:'Skill',attempts:'Attempts',masteryLabel:'Estimated mastery',majorAreas:'Your performance by area',analysis:'Performance analysis',analysisBadge:'AI interprets; the system calculates the numbers',opportunities:'Main opportunities for improvement',plan:'Recommended plan',priority:'Priority',current:'Current performance',recommendation:'Recommendation',startReview:'Start review',trainWeak:'TRAIN MY HIGH-PRIORITY AREAS',trainWeakHelp:'Creates a 20-question adaptive session that prioritizes lower-mastery areas while keeping spaced review of stronger areas.',activity:'Your activity',studiedDays:'Days studied',questionsWeek:'Questions in the last 7 days',avgSession:'Average session time',totalStudy:'Total study time',currentStreak:'Current streak',longestStreak:'Longest streak',xp:'Study XP',xpNote:'XP measures engagement, not clinical competence.',level:'Level',refreshAI:'Refresh AI analysis',aiUnavailable:'Connect Supabase and the AI function for a personalized interpretation. The analysis below is calculated locally from your data.',aiLoading:'Analyzing your data…',aiError:'The AI analysis could not be updated.',emptyTitle:'Your performance will appear here as you use ECG Lab.',emptyBody:'Complete your first training session, practice exam, or clinical case to get started.',startTraining:'Start Training',startSimulation:'Take Practice Exam',openCase:'Open Clinical Case',improving:'Your recent performance is improving.',stable:'Your recent performance is stable.',declining:'Your recent performance has decreased.',needMore:'More responses are needed before consistent patterns can be identified.',noWeakness:'No recurring difficulty reached the minimum of three occurrences in this period.',practice:'Practice',reviewTheory:'Review the theory and then complete targeted questions.',casePractice:'Complete a related clinical case.',focusedReady:'A focused training session was prepared from your highest-priority areas.',completed:'completed',correct:'correct',caseReasoning:'Clinical reasoning',basicInterpretation:'Basic interpretation',rhythmRate:'Rhythm and rate',conduction:'Conduction',arrhythmias:'Arrhythmias',ischemia:'Ischemia',pacing:'Pacing',clinicalReasoning:'Clinical reasoning',noEvidence:'Not enough data',trendUp:'improving',trendDown:'decreasing',trendFlat:'stable',objectiveNote:'Accuracy, Mastery Score, XP, and trends are calculated by the application. AI does not alter these values.'
    }
  };
  const L=()=>text[window.ECG_LANG==='en'?'en':'pt'];
  const isEn=()=>window.ECG_LANG==='en';
  const safe=v=>typeof esc==='function'?esc(v):String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const clamp=(n,a,b)=>Math.max(a,Math.min(b,n));
  const nowIso=()=>new Date().toISOString();
  const userId=()=>state?.user?.id||(state?.demo?'demo-user':'guest');
  const perfKey=()=>PERF_PREFIX+userId();
  const snapKey=()=>SNAP_PREFIX+userId();
  const focusKey=()=>FOCUS_PREFIX+userId();
  const hasCloud=()=>typeof sb!=='undefined'&&!!sb&&!!state?.user&&!state?.demo;
  const fmtPct=n=>Number.isFinite(Number(n))?`${Math.round(Number(n))}%`:'—';
  function fmtDuration(sec){sec=Math.max(0,Math.round(Number(sec)||0));const h=Math.floor(sec/3600),m=Math.floor((sec%3600)/60);if(h)return `${h}h${m?` ${m}m`:''}`;if(m)return `${m}m`;return `${sec}s`}
  function dateKey(v){const d=new Date(v);return Number.isNaN(d.getTime())?'':`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}

  const categoryMap={
    sinus:['Ritmo e frequência','Ritmo sinusal','Ritmo sinusal'],sinus_brady:['Ritmo e frequência','Bradicardia','Frequência e bradicardia'],sinus_tachy:['Ritmo e frequência','Taquicardia','Frequência e taquicardia'],sinus_arrhythmia:['Ritmo e frequência','Regularidade','Regularidade do ritmo'],
    afib:['Arritmias','Fibrilação atrial','Fibrilação atrial'],flutter:['Arritmias','Flutter atrial','Flutter atrial'],svt:['Arritmias','Arritmias supraventriculares','Taquicardia supraventricular'],psvt:['Arritmias','Arritmias supraventriculares','TSV paroxística'],pac:['Arritmias','Ectopia atrial','Extrassístoles atriais'],
    pvc:['Arritmias','Ectopia ventricular','Extrassístoles ventriculares'],bigeminy:['Arritmias','Ectopia ventricular','Bigeminismo ventricular'],trigeminy:['Arritmias','Ectopia ventricular','Trigeminismo ventricular'],vt:['Arritmias','Arritmias ventriculares','Taquicardia ventricular'],vf:['Arritmias','Arritmias ventriculares','Fibrilação ventricular'],
    avb1:['Condução','Bloqueios AV','BAV de 1º grau'],mobitz1:['Condução','Bloqueios AV','Mobitz I'],mobitz2:['Condução','Bloqueios AV','Mobitz II'],avb3:['Condução','Bloqueios AV','Bloqueio AV total'],paced:['Marcapasso','Estimulação cardíaca','Identificação de marcapasso']
  };
  const categoryMapEn={
    sinus:['Rhythm and rate','Sinus rhythm','Sinus rhythm'],sinus_brady:['Rhythm and rate','Bradycardia','Rate and bradycardia'],sinus_tachy:['Rhythm and rate','Tachycardia','Rate and tachycardia'],sinus_arrhythmia:['Rhythm and rate','Regularity','Rhythm regularity'],
    afib:['Arrhythmias','Atrial fibrillation','Atrial fibrillation'],flutter:['Arrhythmias','Atrial flutter','Atrial flutter'],svt:['Arrhythmias','Supraventricular arrhythmias','Supraventricular tachycardia'],psvt:['Arrhythmias','Supraventricular arrhythmias','Paroxysmal SVT'],pac:['Arrhythmias','Atrial ectopy','Premature atrial contractions'],
    pvc:['Arrhythmias','Ventricular ectopy','Premature ventricular contractions'],bigeminy:['Arrhythmias','Ventricular ectopy','Ventricular bigeminy'],trigeminy:['Arrhythmias','Ventricular ectopy','Ventricular trigeminy'],vt:['Arrhythmias','Ventricular arrhythmias','Ventricular tachycardia'],vf:['Arrhythmias','Ventricular arrhythmias','Ventricular fibrillation'],
    avb1:['Conduction','AV block','First-degree AV block'],mobitz1:['Conduction','AV block','Mobitz I'],mobitz2:['Conduction','AV block','Mobitz II'],avb3:['Conduction','AV block','Complete AV block'],paced:['Pacing','Cardiac pacing','Pacemaker identification']
  };
  function classify(category,lessonKey='',fallback=''){
    const map=isEn()?categoryMapEn:categoryMap;if(category&&map[category]){const [topic,subtopic,competency]=map[category];return {topic,subtopic,competency}}
    const k=String(lessonKey||'').toLowerCase();
    if(/isch|st[_-]?t|infar|acs/.test(k))return isEn()?{topic:'Ischemia',subtopic:'ST-T changes',competency:'Ischemia and ST-T interpretation'}:{topic:'Isquemia',subtopic:'Alterações ST-T',competency:'Interpretação de isquemia e ST-T'};
    if(/fund|basic|normal/.test(k))return isEn()?{topic:'Basic interpretation',subtopic:'Systematic interpretation',competency:'Systematic ECG interpretation'}:{topic:'Interpretação básica',subtopic:'Interpretação sistemática',competency:'Interpretação sistemática do ECG'};
    return isEn()?{topic:'Clinical reasoning',subtopic:fallback||'Clinical case',competency:fallback||'Clinical ECG reasoning'}:{topic:'Raciocínio clínico',subtopic:fallback||'Estudo de caso',competency:fallback||'Raciocínio clínico em ECG'};
  }

  function readEvents(){try{const x=JSON.parse(localStorage.getItem(perfKey())||'[]');return Array.isArray(x)?x:[]}catch{return []}}
  function writeEvents(events){try{localStorage.setItem(perfKey(),JSON.stringify(events));return true}catch(e){console.warn('Performance local store:',e);return false}}
  function mergeEvent(event){if(!event?.id)return false;const list=readEvents(),i=list.findIndex(x=>x.id===event.id),normalized={...event,user_id:userId(),updated_at:nowIso()};if(i>=0)list[i]={...list[i],...normalized};else list.push(normalized);list.sort((a,b)=>new Date(a.answered_at||a.created_at)-new Date(b.answered_at||b.created_at));writeEvents(list);scheduleSync();scheduleRender();return true}
  function mergeEvents(events){if(!events?.length)return false;const list=readEvents(),by=new Map(list.map(e=>[e.id,e]));for(const e of events){if(e?.id)by.set(e.id,{...(by.get(e.id)||{}),...e,user_id:userId()})}const out=[...by.values()].sort((a,b)=>new Date(a.answered_at||a.created_at)-new Date(b.answered_at||b.created_at));writeEvents(out);scheduleSync();scheduleRender();return true}

  function currentQuestionMeta(qid,snapshot){const live=(state?.questions||[]).find(q=>String(q.id)===String(qid)),ecg=live&&typeof caseById==='function'?caseById(live.ecg_case_id):null;return {category:live?.category||ecg?.category||snapshot?.category||'',difficulty:Number(live?.difficulty||ecg?.difficulty||snapshot?.difficulty||3),case_id:live?.ecg_case_id||snapshot?.ecg_case_id||null}}
  function backfillTraining(out){let sessions=[];try{sessions=JSON.parse(localStorage.getItem(TRAIN_PREFIX+userId())||'[]')}catch{};if(!Array.isArray(sessions))return;for(const s of sessions){for(const a of s.answers||[]){const cls=classify(a.category,'',a.skill),meta=currentQuestionMeta(a.question_id,null);out.push({id:`quick:${s.id}:${a.sequence||a.question_id}`,activity_id:s.id,activity_type:'quick_training',question_id:String(a.question_id||''),case_id:meta.case_id,topic:cls.topic,subtopic:cls.subtopic,competency:cls.competency,difficulty:Number(a.difficulty||meta.difficulty||3),correct:!!a.is_correct,score:a.is_correct?100:0,response_time:Number(a.response_time)||0,attempt_number:Number(a.sequence)||1,answered_at:a.answered_at||s.updated_at||s.created_at,created_at:a.answered_at||s.created_at||nowIso(),metadata:{category:a.category,skill:a.skill,ability_before:a.ability_before,ability_after:a.ability_after,session_status:s.status,total_questions:s.total_questions}})}}
  function backfillSimulations(out){let sessions=[];try{sessions=JSON.parse(localStorage.getItem(SIM_PREFIX+userId())||'[]')}catch{};if(!Array.isArray(sessions))return;for(const s of sessions){for(const [qid,a] of Object.entries(s.answers||{})){if(!Number.isInteger(a?.selected_answer))continue;const snap=s.question_snapshots?.[qid]||{},meta=currentQuestionMeta(qid,snap),cls=classify(meta.category||snap.category);out.push({id:`simulation:${s.id}:${qid}`,activity_id:s.id,activity_type:'simulation',question_id:String(qid),case_id:meta.case_id,topic:cls.topic,subtopic:cls.subtopic,competency:cls.competency,difficulty:Number(meta.difficulty||3),correct:!!a.is_correct,score:a.is_correct?100:0,response_time:Number(a.response_time)||0,attempt_number:1,answered_at:a.answered_at||s.updated_at||s.created_at,created_at:a.answered_at||s.created_at||nowIso(),metadata:{category:meta.category||snap.category||'',session_status:s.status,activity_completed:s.status==='completed',total_questions:s.total_questions,score_percentage:s.score_percentage}})}}
  function findStudyCase(id){const d=window.ECG_STUDY_CONTENT||{};for(const [lessonKey,l] of Object.entries(d.lessons||{})){const c=(l.cases||[]).find(x=>String(x.id)===String(id));if(c)return {lessonKey,lesson:l,case:c}}return null}
  function caseCategory(ref){const url=String(ref?.case?.ecg_image||'');const m=url.match(/assets\/ecg\/([^/]+)\//);return m?.[1]||''}
  function backfillCases(out){for(let i=0;i<localStorage.length;i++){const key=localStorage.key(i)||'';if(!key.startsWith(CASE_PREFIX))continue;let v;try{v=JSON.parse(localStorage.getItem(key)||'null')}catch{};if(!v?.feedback)return;const parts=key.split(':'),caseId=parts.slice(2).join(':'),ref=findStudyCase(caseId),cat=caseCategory(ref),cls=classify(cat,ref?.lessonKey,ref?.lesson?.title),score=clamp(Number(v.feedback.score)||0,0,100),at=Number(v.at)||Date.now();out.push({id:`clinical_case:${caseId}:${at}`,activity_id:ref?.lessonKey||caseId,activity_type:'clinical_case',question_id:null,case_id:caseId,topic:cls.topic,subtopic:cls.subtopic,competency:cls.competency,difficulty:Number(ref?.case?.difficulty||3),correct:score>=70,score,response_time:0,attempt_number:1,answered_at:new Date(at).toISOString(),created_at:new Date(at).toISOString(),metadata:{clinical_reasoning_score:score,user_reasoning:v.answer||'',ai_feedback:v.feedback,diagnosis:ref?.case?.answer||'',category:cat}})}}
  function backfillSources(){const out=[];backfillTraining(out);backfillSimulations(out);backfillCases(out);mergeEvents(out);return out.length}

  function filteredEvents(period=runtime.period,source=readEvents()){const days=PERIODS[period];if(!days)return source;const since=Date.now()-days*86400000;return source.filter(e=>new Date(e.answered_at||e.created_at).getTime()>=since)}
  function eventValue(e){if(e.activity_type==='clinical_case')return clamp(Number(e.score)||0,0,100);return e.correct?100:0}
  function eventWeight(e){const d=clamp(Number(e.difficulty)||3,1,5),age=Math.max(0,(Date.now()-new Date(e.answered_at||e.created_at))/86400000);return (0.78+d*.09)*(0.58+0.42*Math.exp(-age/180))}
  function shrunkScore(events,prior=60,priorWeight=5){if(!events.length)return prior;let sw=priorWeight,sum=prior*priorWeight;for(const e of events){const w=eventWeight(e);sw+=w;sum+=eventValue(e)*w}return clamp(sum/sw,0,100)}
  function eligibleEvent(e){return e.activity_type!=='simulation'||e.metadata?.activity_completed===true}
  function componentScores(all){const eligible=all.filter(eligibleEvent),quick=eligible.filter(e=>e.activity_type==='quick_training'),sim=eligible.filter(e=>e.activity_type==='simulation'),cases=eligible.filter(e=>e.activity_type==='clinical_case');return {quick:shrunkScore(quick),simulation:shrunkScore(sim),clinical:shrunkScore(cases),quickN:quick.length,simN:sim.length,caseN:cases.length}}
  function activeDays(events){return new Set(events.map(e=>dateKey(e.answered_at||e.created_at)).filter(Boolean))}
  function recentBase(all,fromDays,toDays){const now=Date.now(),from=now-fromDays*86400000,to=now-toDays*86400000,ev=all.filter(e=>eligibleEvent(e)&&new Date(e.answered_at||e.created_at).getTime()>=from&&new Date(e.answered_at||e.created_at).getTime()<to);return ev.length?shrunkScore(ev):null}
  function progressionScore(all){const current=recentBase(all,30,0),previous=recentBase(all,60,30),trend=current!=null&&previous!=null?current-previous:0,days=activeDays(all.filter(e=>new Date(e.answered_at||e.created_at).getTime()>Date.now()-30*86400000)).size,consistency=clamp(days/12,0,1);return {score:clamp(55+trend*1.4+consistency*20,0,100),trend}}
  function masteryFrom(all){const c=componentScores(all),p=progressionScore(all),mastery=.30*c.simulation+.25*c.quick+.30*c.clinical+.15*p.score,total=c.quickN+c.simN+c.caseN,types=[c.quickN>0,c.simN>0,c.caseN>0].filter(Boolean).length,confidence=total>=40&&types>=2?'high':total>=15?'medium':'low';return {...c,progression:p.score,trend:p.trend,mastery:clamp(mastery,0,100),total,types,confidence}}
  function skillScores(all){const groups=new Map();for(const e of all.filter(eligibleEvent)){const k=e.competency||e.subtopic||e.topic||'Other';if(!groups.has(k))groups.set(k,[]);groups.get(k).push(e)}return [...groups].map(([name,events])=>({name,score:shrunkScore(events),attempts:events.length,topic:events[0]?.topic||'',category:events[0]?.metadata?.category||''})).sort((a,b)=>b.attempts-a.attempts||b.score-a.score)}
  function areaScores(all){const groups=new Map();for(const e of all.filter(eligibleEvent)){const k=e.topic||'Other';if(!groups.has(k))groups.set(k,[]);groups.get(k).push(e)}return [...groups].map(([name,events])=>({name,score:shrunkScore(events),attempts:events.length})).sort((a,b)=>b.attempts-a.attempts)}
  function categoryScores(all){const groups=new Map();for(const e of all.filter(eligibleEvent)){const k=e.metadata?.category;if(!k)continue;if(!groups.has(k))groups.set(k,[]);groups.get(k).push(e)}return [...groups].map(([category,events])=>({category,score:shrunkScore(events),attempts:events.length,errors:events.filter(e=>eventValue(e)<60).length})).sort((a,b)=>a.score-b.score)}
  function recurringErrors(all){const groups=new Map();for(const e of all.filter(eligibleEvent)){const k=e.competency||e.subtopic;if(!k)continue;const g=groups.get(k)||{name:k,total:0,errors:0};g.total++;if(eventValue(e)<60)g.errors++;groups.set(k,g)}return [...groups.values()].filter(g=>g.errors>=3&&g.total>=3).sort((a,b)=>b.errors/a.total-a.errors/b.total)}
  function sessionStats(){let t=[],s=[];try{t=JSON.parse(localStorage.getItem(TRAIN_PREFIX+userId())||'[]')}catch{};try{s=JSON.parse(localStorage.getItem(SIM_PREFIX+userId())||'[]')}catch{};return {training:Array.isArray(t)?t:[],simulations:Array.isArray(s)?s:[]}}
  function streakStats(events){const days=[...activeDays(events)].sort(),set=new Set(days);let current=0,longest=0,run=0,prev=null;for(const key of days){const d=new Date(key+'T12:00:00');if(prev&&Math.round((d-prev)/86400000)===1)run++;else run=1;longest=Math.max(longest,run);prev=d}let cursor=new Date();cursor.setHours(12,0,0,0);for(;;){const k=dateKey(cursor);if(set.has(k)){current++;cursor=new Date(cursor.getTime()-86400000)}else{if(current===0){const y=new Date(cursor.getTime()-86400000);if(set.has(dateKey(y))){cursor=y;continue}}break}}return {current,longest}}
  const levelNamesPt=['','ECG Iniciante','Aprendiz de ECG','Explorador de Ritmos','Leitor de ECG','Intérprete de ECG','Intérprete Habilidoso','Intérprete Avançado de ECG','Analista Clínico de ECG','Especialista em ECG','Mestre do ECG'];
  const levelNamesEn=['','ECG Beginner','ECG Learner','Rhythm Explorer','ECG Reader','ECG Interpreter','Skilled Interpreter','Advanced ECG Interpreter','Clinical ECG Analyst','ECG Expert','ECG Master'];
  function xpStats(all){const ss=sessionStats(),completedTraining=ss.training.filter(x=>x.status==='completed').length,completedSims=ss.simulations.filter(x=>x.status==='completed').length,cases=all.filter(e=>e.activity_type==='clinical_case').length,answered=all.filter(e=>e.activity_type!=='clinical_case').length,st=streakStats(all),xp=answered*3+completedTraining*30+completedSims*45+cases*35+Math.min(st.longest,30)*8,level=clamp(Math.floor(xp/1000)+1,1,10),base=(level-1)*1000,progress=xp-base;return {xp,level,progress,next:1000,name:(isEn()?levelNamesEn:levelNamesPt)[level]}}
  function activityStats(all){const ss=sessionStats(),completed=[...ss.training,...ss.simulations].filter(x=>x.status==='completed'),totalSec=completed.reduce((n,x)=>n+Number(x.elapsed_seconds||x.elapsed_time||0),0),avg=completed.length?totalSec/completed.length:0,days=activeDays(all).size,last7=all.filter(e=>new Date(e.answered_at||e.created_at)>new Date(Date.now()-7*86400000)&&e.activity_type!=='clinical_case').length,st=streakStats(all);return {days,last7,totalSec,avg,...st,completedTraining:ss.training.filter(x=>x.status==='completed').length,completedSims:ss.simulations.filter(x=>x.status==='completed').length}}
  function confidenceLabel(c){return c==='high'?L().highConfidence:c==='medium'?L().mediumConfidence:L().lowConfidence}

  function evolutionPoints(all,period){const ev=filteredEvents(period,all).filter(eligibleEvent).sort((a,b)=>new Date(a.answered_at)-new Date(b.answered_at));if(ev.length<2)return[];const byDay=new Map();for(const e of ev){const k=dateKey(e.answered_at||e.created_at);if(!byDay.has(k))byDay.set(k,[]);byDay.get(k).push(e)}const keys=[...byDay.keys()].sort(),acc=[];return keys.map(k=>{acc.push(...byDay.get(k));return {date:k,value:shrunkScore(acc)}})}
  function lineChart(points){if(points.length<2)return `<p class="muted">${safe(L().noChart)}</p>`;const w=900,h=280,p=42,x=i=>p+i*(w-2*p)/Math.max(1,points.length-1),y=v=>p+(100-v)*(h-2*p)/100,poly=points.map((z,i)=>`${x(i)},${y(z.value)}`).join(' '),grid=[0,25,50,75,100].map(v=>`<line x1="${p}" y1="${y(v)}" x2="${w-p}" y2="${y(v)}" class="perf-grid"/><text x="${p-8}" y="${y(v)+4}" text-anchor="end">${v}%</text>`).join('');return `<div class="perf-chart-wrap"><svg class="perf-chart" viewBox="0 0 ${w} ${h}" role="img" aria-label="${safe(L().progress)}">${grid}<polyline class="perf-line" points="${poly}"/>${points.map((z,i)=>`<circle class="perf-dot" cx="${x(i)}" cy="${y(z.value)}" r="4"><title>${z.date}: ${Math.round(z.value)}%</title></circle>`).join('')}</svg></div>`}
  function skillBand(score){return score>=80?'excellent':score>=70?'good':score>=60?'fair':score>=50?'watch':'priority'}
  function skillTable(rows){if(!rows.length)return `<p class="muted">${safe(L().needMore)}</p>`;return `<div class="perf-skill-list">${rows.map(r=>`<div class="perf-skill-row ${skillBand(r.score)}"><div><strong>${safe(r.name)}</strong><small>${r.attempts} ${safe(L().attempts.toLowerCase())}</small></div><div class="perf-skill-bar"><i style="width:${Math.round(r.score)}%"></i></div><b>${Math.round(r.score)}%</b></div>`).join('')}</div>`}
  function areaGrid(rows){const desired=isEn()?['Basic interpretation','Rhythm and rate','Conduction','Arrhythmias','Ischemia','Pacing','Clinical reasoning']:['Interpretação básica','Ritmo e frequência','Condução','Arritmias','Isquemia','Marcapasso','Raciocínio clínico'];const map=new Map(rows.map(x=>[x.name,x]));return `<div class="perf-area-grid">${desired.map(name=>{const r=map.get(name);return `<div class="perf-area"><span>${safe(name)}</span><strong>${r?Math.round(r.score):'—'}${r?'%':''}</strong><small>${r?`${r.attempts} ${safe(L().attempts.toLowerCase())}`:safe(L().noEvidence)}</small></div>`}).join('')}</div>`}
  function trendSentence(t){return t>2?L().improving:t<-2?L().declining:L().stable}
  function localAnalysis(model){const weak=model.skills.filter(x=>x.attempts>=3).sort((a,b)=>a.score-b.score).slice(0,2),errors=model.errors.slice(0,2),parts=[trendSentence(model.mastery.trend)];if(weak.length)parts.push(isEn()?`The lowest current mastery estimates are ${weak.map(x=>x.name).join(' and ')}.`:`As menores estimativas atuais de domínio estão em ${weak.map(x=>x.name).join(' e ')}.`);if(errors.length)parts.push(isEn()?`${errors.map(x=>x.name).join(' and ')} show recurring errors across at least three attempts.`:`${errors.map(x=>x.name).join(' e ')} apresentam erros recorrentes em pelo menos três tentativas.`);else parts.push(L().noWeakness);return parts.join(' ')}
  function planRows(model){let rows=model.skills.filter(x=>x.attempts>=3).sort((a,b)=>a.score-b.score).slice(0,3);if(!rows.length)rows=model.skills.slice().sort((a,b)=>a.score-b.score).slice(0,3);return rows}
  function planHtml(model){const rows=planRows(model);if(!rows.length)return `<p class="muted">${safe(L().needMore)}</p>`;return `<div class="perf-plan-list">${rows.map((r,i)=>`<article class="perf-plan-item"><span class="badge">${safe(L().priority)} ${i+1}</span><h4>${safe(r.name)}</h4><p>${safe(L().current)}: <strong>${Math.round(r.score)}%</strong> • ${r.attempts} ${safe(L().attempts.toLowerCase())}</p><div class="perf-plan-actions"><span>1. ${safe(L().reviewTheory)}</span><span>2. ${safe(L().practice)} ${Math.max(5,10-i*2)} ${safe(L().questionsAnswered.toLowerCase())}</span><span>3. ${safe(L().casePractice)}</span></div></article>`).join('')}</div>`}
  function buildModel(period=runtime.period){const all=readEvents(),view=filteredEvents(period,all),mastery=masteryFrom(view),skills=skillScores(view),areas=areaScores(view),errors=recurringErrors(view),activity=activityStats(all),xp=xpStats(all),categories=categoryScores(view),eligible=view.filter(eligibleEvent),binary=eligible.filter(e=>e.activity_type!=='clinical_case'),accuracy=binary.length?binary.filter(e=>e.correct).length/binary.length*100:0,cases=eligible.filter(e=>e.activity_type==='clinical_case').length,points=evolutionPoints(all,period);return {all,view,mastery,skills,areas,errors,activity,xp,categories,eligible,accuracy,cases,points}}

  function saveDailySnapshot(model){if(!model.all.length)return;const key=dateKey(new Date()),snap={snapshot_date:key,mastery_score:Number(model.mastery.mastery.toFixed(2)),simulation_score:Number(model.mastery.simulation.toFixed(2)),training_score:Number(model.mastery.quick.toFixed(2)),clinical_case_score:Number(model.mastery.clinical.toFixed(2)),created_at:nowIso()};let list=[];try{list=JSON.parse(localStorage.getItem(snapKey())||'[]')}catch{};if(!Array.isArray(list))list=[];const i=list.findIndex(x=>x.snapshot_date===key);if(i>=0)list[i]=snap;else list.push(snap);localStorage.setItem(snapKey(),JSON.stringify(list.slice(-365)));return snap}

  function renderEmpty(el){el.innerHTML=`<div class="perf-empty card"><div class="perf-empty-icon">↗</div><h2>${safe(L().emptyTitle)}</h2><p>${safe(L().emptyBody)}</p><div class="cta"><button class="btn btn-primary" data-perf-page="treinar">${safe(L().startTraining)}</button><button class="btn btn-secondary" data-perf-page="simulados">${safe(L().startSimulation)}</button><button class="btn btn-ghost" data-perf-page="trilha">${safe(L().openCase)}</button></div></div>`;el.querySelectorAll('[data-perf-page]').forEach(b=>b.onclick=()=>showPage(b.dataset.perfPage))}
  function renderPerformance(){const el=document.getElementById('desempenho');if(!el)return;backfillSources();const model=buildModel();if(!model.all.length){renderEmpty(el);return}saveDailySnapshot(model);const m=model.mastery,delta=m.trend,deltaLabel=`${delta>0?'↑ ':delta<0?'↓ ':''}${delta>0?'+':''}${delta.toFixed(1)} pp`,ai=readAiInsight(),analysis=ai?.summary||localAnalysis(model),recur=model.errors.slice(0,4);el.innerHTML=`<div class="perf-page"><div class="perf-head"><div><span class="eyebrow">Analytics</span><h2>${safe(L().title)}</h2><p>${safe(L().subtitle)}</p></div><div class="perf-periods" aria-label="${safe(L().filters)}">${[['d7',L().days7],['d30',L().days30],['d90',L().days90],['all',L().allTime]].map(([k,v])=>`<button class="chip${runtime.period===k?' active':''}" data-perf-period="${k}">${safe(v)}</button>`).join('')}</div></div><article class="card perf-mastery"><div class="perf-mastery-score"><span>${safe(L().mastery)}</span><strong>${Math.round(m.mastery)}<small>/100</small></strong><div class="perf-confidence ${m.confidence}">${safe(confidenceLabel(m.confidence))}</div><p>${safe(L().objectiveNote)}</p></div><div class="perf-mastery-change"><span>${safe(L().change)}</span><strong class="${delta>2?'up':delta<-2?'down':'flat'}">${safe(deltaLabel)}</strong><small>${safe(runtime.period==='d7'?L().days7:runtime.period==='d90'?L().days90:L().days30)}</small></div>${m.confidence==='low'?`<div class="perf-confidence-note"><strong>${safe(L().insufficient)}</strong><p>${safe(L().insufficientBody)}</p></div>`:''}</article><div class="grid perf-kpis"><article class="card stat"><span>${safe(L().questionsAnswered)}</span><strong>${model.eligible.filter(e=>e.activity_type!=='clinical_case').length}</strong></article><article class="card stat"><span>${safe(L().accuracy)}</span><strong>${fmtPct(model.accuracy)}</strong></article><article class="card stat"><span>${safe(L().simulations)}</span><strong>${model.activity.completedSims}</strong></article><article class="card stat"><span>${safe(L().clinicalCases)}</span><strong>${model.cases}</strong></article><article class="card stat"><span>${safe(L().studyTime)}</span><strong>${fmtDuration(model.activity.totalSec)}</strong></article><article class="card stat"><span>${safe(L().streak)}</span><strong>${model.activity.current} ${safe(model.activity.current===1?L().day:L().days)}</strong></article></div><article class="card section perf-progress"><div class="section-head"><div><h3>${safe(L().progress)}</h3><p class="muted">${safe(trendSentence(delta))}</p></div></div>${lineChart(model.points)}</article><div class="grid perf-two"><article class="card section"><div class="section-head"><h3>${safe(L().skills)}</h3></div>${skillTable(model.skills)}</article><article class="card section"><div class="section-head"><h3>${safe(L().majorAreas)}</h3></div>${areaGrid(model.areas)}</article></div><article class="card section perf-analysis"><div class="section-head"><div><h3>${safe(L().analysis)}</h3><p class="muted">${safe(L().analysisBadge)}</p></div><button class="btn btn-secondary" id="perfAiRefresh" ${hasCloud()&&m.total>=5?'':'disabled'}>${safe(L().refreshAI)}</button></div><p class="perf-analysis-text" id="perfAiText">${safe(analysis)}</p>${!hasCloud()?`<p class="muted">${safe(L().aiUnavailable)}</p>`:''}${recur.length?`<div class="perf-errors"><strong>${safe(L().opportunities)}</strong>${recur.map(x=>`<span>${safe(x.name)} — ${x.errors}/${x.total}</span>`).join('')}</div>`:''}</article><article class="card section"><div class="section-head"><h3>${safe(L().plan)}</h3></div>${planHtml(model)}<div class="perf-focus-cta"><div><strong>${safe(L().trainWeak)}</strong><p>${safe(L().trainWeakHelp)}</p></div><button class="btn btn-primary" id="perfTrainWeak" ${model.categories.length?'':'disabled'}>${safe(L().startReview)}</button></div></article><div class="grid perf-two"><article class="card section"><div class="section-head"><h3>${safe(L().activity)}</h3></div><div class="perf-activity-grid"><div><span>${safe(L().studiedDays)}</span><strong>${model.activity.days}</strong></div><div><span>${safe(L().questionsWeek)}</span><strong>${model.activity.last7}</strong></div><div><span>${safe(L().avgSession)}</span><strong>${fmtDuration(model.activity.avg)}</strong></div><div><span>${safe(L().totalStudy)}</span><strong>${fmtDuration(model.activity.totalSec)}</strong></div><div><span>${safe(L().currentStreak)}</span><strong>${model.activity.current}</strong></div><div><span>${safe(L().longestStreak)}</span><strong>${model.activity.longest}</strong></div></div></article><article class="card section perf-xp"><div class="section-head"><h3>${safe(L().xp)}</h3><span class="badge">${safe(L().level)} ${model.xp.level}</span></div><h4>${safe(model.xp.name)}</h4><strong>${model.xp.xp.toLocaleString(isEn()?'en-US':'pt-BR')} XP</strong><div class="perf-xp-bar"><i style="width:${Math.min(100,model.xp.progress/10)}%"></i></div><small>${model.xp.progress} / ${model.xp.next} XP</small><p class="muted">${safe(L().xpNote)}</p></article></div></div>`;el.querySelectorAll('[data-perf-period]').forEach(b=>b.onclick=()=>{runtime.period=b.dataset.perfPeriod;renderPerformance()});document.getElementById('perfTrainWeak')?.addEventListener('click',()=>startWeakTraining(model));document.getElementById('perfAiRefresh')?.addEventListener('click',()=>refreshAiInsight(model));void syncAggregate(model)}

  function focusWeights(model){const cats=model.categories.filter(x=>x.attempts>=2);if(!cats.length)return{};const raw=cats.map(x=>({category:x.category,priority:Math.max(5,100-x.score)*(1+Math.min(x.errors,5)*.12)})),max=Math.max(...raw.map(x=>x.priority));return Object.fromEntries(raw.map(x=>[x.category,Number((x.priority/max).toFixed(3))]))}
  function startWeakTraining(model){const weights=focusWeights(model);if(!Object.keys(weights).length)return;try{localStorage.setItem(focusKey(),JSON.stringify({weights,created_at:nowIso(),expires_at:new Date(Date.now()+2*3600000).toISOString(),source:'my_performance'}))}catch{};toast?.(L().focusedReady);showPage('treinar');setTimeout(()=>{if(window.ECG_CAT_TRAINING?.startSession)window.ECG_CAT_TRAINING.startSession(20)},80)}

  function readAiInsight(){try{return JSON.parse(localStorage.getItem(`ecgLabPerformanceInsight:v1:${userId()}:${window.ECG_LANG||'pt-BR'}`)||'null')}catch{return null}}
  function writeAiInsight(v){try{localStorage.setItem(`ecgLabPerformanceInsight:v1:${userId()}:${window.ECG_LANG||'pt-BR'}`,JSON.stringify(v))}catch{}}
  async function refreshAiInsight(model){if(runtime.aiBusy||!hasCloud())return;runtime.aiBusy=true;const btn=document.getElementById('perfAiRefresh'),box=document.getElementById('perfAiText');if(btn)btn.disabled=true;if(box)box.textContent=L().aiLoading;try{const {data:{session}}=await sb.auth.getSession();if(!session)throw new Error('No session');const payload={mode:'performance-insight',language:isEn()?'en':'pt-BR',performance:{mastery:Number(model.mastery.mastery.toFixed(1)),trend:Number(model.mastery.trend.toFixed(1)),confidence:model.mastery.confidence,componentScores:{simulation:Number(model.mastery.simulation.toFixed(1)),quickTraining:Number(model.mastery.quick.toFixed(1)),clinicalCases:Number(model.mastery.clinical.toFixed(1)),progression:Number(model.mastery.progression.toFixed(1))},weakestCompetencies:model.skills.slice().sort((a,b)=>a.score-b.score).slice(0,5).map(x=>({name:x.name,score:Number(x.score.toFixed(1)),attempts:x.attempts})),recentErrors:model.errors.slice(0,5),clinicalReasoningPatterns:model.all.filter(e=>e.activity_type==='clinical_case').slice(-6).map(e=>({competency:e.competency,score:e.score,error_reasons:e.metadata?.ai_feedback?.error_reasons||[]}))}};const res=await fetch(`${CFG.SUPABASE_URL}/functions/v1/performance-insight`,{method:'POST',headers:{'Content-Type':'application/json','Authorization':`Bearer ${session.access_token}`},body:JSON.stringify(payload)}),data=await res.json();if(!res.ok)throw new Error(data.error||L().aiError);const insight={...(data.insight||{}),at:nowIso()};writeAiInsight(insight);if(box)box.textContent=insight.summary||localAnalysis(model);if(insight.summary)try{await sb.from('ai_performance_insights').insert({user_id:state.user.id,language:isEn()?'en':'pt-BR',input_snapshot:payload.performance,summary:insight.summary,recommendations:insight.recommendations||[],created_at:nowIso()})}catch{}}catch(e){console.warn('Performance AI:',e);if(box)box.textContent=localAnalysis(model);toast?.(L().aiError)}finally{runtime.aiBusy=false;if(btn)btn.disabled=!hasCloud()}}

  function remoteRow(e){return {id:e.id,user_id:state.user.id,activity_id:String(e.activity_id||''),activity_type:e.activity_type,question_id:e.question_id?String(e.question_id):null,case_id:e.case_id?String(e.case_id):null,topic:e.topic||null,subtopic:e.subtopic||null,competency:e.competency||null,difficulty:Number(e.difficulty)||null,correct:typeof e.correct==='boolean'?e.correct:null,score:Number.isFinite(Number(e.score))?Number(e.score):null,response_time:Math.round(Number(e.response_time)||0),attempt_number:Number(e.attempt_number)||1,answered_at:e.answered_at||nowIso(),metadata:e.metadata||{},created_at:e.created_at||e.answered_at||nowIso(),updated_at:e.updated_at||nowIso()}}
  async function loadRemote(){if(!hasCloud()||runtime.remoteLoadedFor===state.user.id)return;runtime.remoteLoadedFor=state.user.id;try{const {data,error}=await sb.from('learning_events').select('*').eq('user_id',state.user.id).order('answered_at',{ascending:true});if(error)throw error;if(data?.length)mergeEvents(data)}catch(e){console.warn('Performance remote load:',e)}}
  function scheduleSync(){if(!hasCloud())return;clearTimeout(runtime.syncTimer);runtime.syncTimer=setTimeout(syncAll,900)}
  async function syncAll(){if(!hasCloud())return;const events=readEvents();if(!events.length)return;try{const {error}=await sb.from('learning_events').upsert(events.map(remoteRow),{onConflict:'id'});if(error)throw error}catch(e){console.warn('Performance event sync:',e)}}
  async function syncAggregate(model){if(!hasCloud())return;try{const m=model.mastery,a=model.activity,x=model.xp,all=model.all,eligible=model.eligible,overall=eligible.filter(e=>e.activity_type!=='clinical_case'),row={user_id:state.user.id,overall_accuracy:overall.length?overall.filter(e=>e.correct).length/overall.length*100:null,mastery_score:m.mastery,quick_training_score:m.quick,simulation_score:m.simulation,clinical_case_score:m.clinical,progression_score:m.progression,total_questions:overall.length,total_cases:all.filter(e=>e.activity_type==='clinical_case').length,total_simulations:a.completedSims,study_time:Math.round(a.totalSec),current_streak:a.current,longest_streak:a.longest,confidence:m.confidence,updated_at:nowIso()};await sb.from('user_performance').upsert(row,{onConflict:'user_id'});const comps=model.skills.map(s=>({user_id:state.user.id,competency:s.name,topic:s.topic||null,attempts:s.attempts,mastery_score:s.score,updated_at:nowIso()}));if(comps.length)await sb.from('competency_performance').upsert(comps,{onConflict:'user_id,competency'});await sb.from('user_xp').upsert({user_id:state.user.id,xp:x.xp,level:x.level,updated_at:nowIso()},{onConflict:'user_id'});const snap=saveDailySnapshot(model);if(snap)await sb.from('performance_snapshots').upsert({user_id:state.user.id,...snap},{onConflict:'user_id,snapshot_date'})}catch(e){console.warn('Performance aggregate sync:',e)}}

  function injectStylesheet(){if(document.querySelector('link[href="performance_features.css"]'))return;const l=document.createElement('link');l.rel='stylesheet';l.href='performance_features.css';document.head.appendChild(l)}
  function injectPerformanceUI(){const shellEl=document.querySelector('.shell');if(!shellEl)return false;const main=document.querySelector('.main'),simSection=document.getElementById('simulados');if(main&&!document.getElementById('desempenho')){const sec=document.createElement('section');sec.id='desempenho';sec.className='page';simSection?.after(sec)}const side=document.querySelector('.sidebar .nav');if(side&&!side.querySelector('[data-page="desempenho"]')){const ref=side.querySelector('[data-page="simulados"]'),b=document.createElement('button');b.dataset.page='desempenho';b.innerHTML=`<b>↗</b><span>${safe(L().nav)}</span>`;b.onclick=()=>showPage('desempenho');ref?.after(b)}const mobile=document.querySelector('.mobile-nav');if(mobile&&!mobile.querySelector('[data-page="desempenho"]')){const ref=mobile.querySelector('[data-page="simulados"]'),b=document.createElement('button');b.dataset.page='desempenho';b.innerHTML=`↗<br><small>${safe(L().mobile)}</small>`;b.onclick=()=>showPage('desempenho');ref?.after(b);mobile.classList.add('has-performance')};return true}
  const originalShow=typeof showPage==='function'?showPage:null;
  if(originalShow){window.showPage=function(id){injectPerformanceUI();const r=originalShow(id);if(id==='desempenho'){const title=document.getElementById('pageTitle'),sub=document.getElementById('pageSubtitle');if(title)title.textContent=L().title;if(sub)sub.textContent=L().subtitle;renderPerformance();void loadRemote().then(()=>renderPerformance())}return r}}
  const originalShell=typeof shell==='function'?shell:null;
  if(originalShell){window.shell=function(){const r=originalShell();injectPerformanceUI();if(state?.page==='desempenho'){setTimeout(()=>showPage('desempenho'),0)}return r}}
  const originalRenderAll=typeof renderAll==='function'?renderAll:null;
  if(originalRenderAll){window.renderAll=function(){const r=originalRenderAll();injectPerformanceUI();renderPerformance();return r}}

  function relevantStorageKey(key){return key===TRAIN_PREFIX+userId()||key===SIM_PREFIX+userId()||String(key).startsWith(CASE_PREFIX)}
  if(!window.__ecgPerformanceStorageHook&&window.Storage){const originalSet=Storage.prototype.setItem;Storage.prototype.setItem=function(key,value){const r=originalSet.call(this,key,value);if(this===localStorage&&relevantStorageKey(String(key)))setTimeout(()=>{backfillSources();if(state?.page==='desempenho')renderPerformance()},0);return r};window.__ecgPerformanceStorageHook=true}
  function scheduleRender(){if(runtime.renderQueued)return;runtime.renderQueued=true;setTimeout(()=>{runtime.renderQueued=false;if(state?.page==='desempenho')renderPerformance()},80)}

  injectStylesheet();
  const boot=()=>{injectPerformanceUI();backfillSources();if(state?.page==='desempenho')renderPerformance();void loadRemote().then(()=>{if(state?.page==='desempenho')renderPerformance()})};
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot);else setTimeout(boot,0);
  window.addEventListener('load',boot);
  window.ECG_PERFORMANCE={version:VERSION,readEvents,backfillSources,buildModel,render:renderPerformance,focusWeights};
})();
